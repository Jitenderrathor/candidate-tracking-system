# Features

Future business features should be isolated here by domain. Each feature may own its API hooks, components, schemas, and utilities while importing shared primitives from `components/common`.
